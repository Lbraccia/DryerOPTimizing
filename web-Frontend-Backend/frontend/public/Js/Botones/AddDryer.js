function AddRed(e){
    // Obtengo el dato
    //  Incorporar un popup para incluir un nuevo nombre
    idredValue = idredValue + 1 
    console.log(idredValue)

    // Tengo que tomar la cantidad de redes creadas para cada usuario y aumentarla en 1. 

    /* //SelectMapRegular()
    // Envio el centro del mapa al backend
    var center = map.getCenter();
    var zoom = map.getZoom();
    var zoomScale = map.getZoomScale(map.getZoom(), zoomReference);
    var dataCrearParametros = {
      idred: idredValue, // Id de la red
      // Nuevos Valores
      P: 1,
      N: 0, 
      NG1: 0,
      NG2: 0,
      NG3: 0,
      S: 0,
      NLoad: 0,
      NStorage: 0,
      NLine: 0,
      Nnodos: 0,
      Layer: Layer, // Capa del mapa
      centerLat:  parseFloat(center.lat), // Lat del centro del mapa
      centerLng:  parseFloat(center.lng), // Lng del centro del mapa
      zoom: zoom,
      zoomScale: zoomScale,
      ColorLine: '#4285f4', // Color de la linea
      WidthLine: 3,  // ancho de la linea
      ColorNode: '#4285f4', // color de la linea
      WidthNode: 0,//2.75 // ancho de la linea
      pinColor: '#4285f4', // color de los pines
      NS: 10, // Number of Discretization Point for Voltage Linearization
      NQP: 350, // Number of Discretization Point for P and Q Linearization
      kw_W: 1, // Cambio de unidad de kW a W
      Omhs_Komhs: 1e-3, //Cambio de unidad de kW a W
      Vnom: 23, // Voltaje nominal (kW)
      ke: 168, // US$/kW-year
      kI: 1000, //US$/kW-year (Inversion Cost)
    }
    CrearParametrosBackend(dataCrearParametros)
    console.log("dataCrearParametros", dataCrearParametros)
    openPopupParameters(e) */



/* 
    // 
     */

}